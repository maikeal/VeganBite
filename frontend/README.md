# VeganBite Frontend

This is the frontend for the full-stack VeganBite restaurant website. It uses React files, Bootstrap, custom CSS, and JavaScript.

The menu now loads from the backend API. Cart updates are saved through the backend, and checkout sends order data to MongoDB.

Update `api-config.js` after deploying your backend.
